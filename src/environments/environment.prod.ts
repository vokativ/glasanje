// Production companion to the legacy Angular CLI environment file. It is not
// consumed by the current Vite entry point, but remains paired with
// environment.ts for the older replacement-based build path.
export const environment = {
  production: true
};
